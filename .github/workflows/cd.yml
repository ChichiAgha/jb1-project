name: CD

on:
  push:
    branches:
      - main
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Docker Compose config
        run: docker compose config

      - name: Build and start stack
        run: docker compose up -d --build --wait

      - name: Run smoke tests
        run: sh scripts/smoke-test.sh

      - name: Run backend CRUD tests
        run: sh scripts/crud-test.sh

      - name: Show logs on failure
        if: failure()
        run: docker compose logs --no-color

      - name: Stop test stack
        if: always()
        run: docker compose down -v

      - name: Set image namespace
        run: echo "IMAGE_NAMESPACE=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend-react
          push: true
          tags: |
            ghcr.io/${{ env.IMAGE_NAMESPACE }}/taskapp-frontend:${{ github.sha }}
            ghcr.io/${{ env.IMAGE_NAMESPACE }}/taskapp-frontend:latest

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: ./backend-php
          push: true
          tags: |
            ghcr.io/${{ env.IMAGE_NAMESPACE }}/taskapp-backend:${{ github.sha }}
            ghcr.io/${{ env.IMAGE_NAMESPACE }}/taskapp-backend:latest
